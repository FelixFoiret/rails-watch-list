<div class="container">

  <h1>Add a list</h1>
  <%= simple_form_for (@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit class: 'btn btn-primary' %>
  <%end %>

  <%= link_to 'All Lists', lists_path %>

</div>
